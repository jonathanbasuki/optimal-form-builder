<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>
        <%= title %>
    </title>

    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>

    <style>
        /* Custom font sizes for headings as per design */
        .heading-1 {
            @apply text-[3rem] font-extrabold leading-tight;
        }

        .heading-2 {
            @apply text-2xl font-semibold;
        }
    </style>
</head>

<body class="bg-white text-gray-600 min-h-screen flex flex-col">

    <!-- Wrapper -->
    <div class="flex flex-1 min-h-0 max-w-screen-xl mx-auto w-full">

        <!-- Sidebar -->
        <%- include('./partials/sidebar') %>


            <!-- Main content -->
            <div class="flex-1 flex flex-col min-h-screen ml-0 md:ml-64">
                <!-- Top navbar -->
                <%- include('./partials/navbar') %>

                    <main class="flex-1 overflow-y-auto p-6 bg-gray-50" id="mainContent" tabindex="-1">
                        <%- body %>
                    </main>
            </div>
    </div>

    <script src="/js/sidebar.js"></script>
    <script src="/js/helpers.js"></script>
</body>

</html>